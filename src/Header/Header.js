import './Header.css';
import { NavLink } from 'react-router-dom';

function Header() {
    return (
      <header>
        <img src="navLogo.svg"></img>
      </header>
    );
}

export default Header;